{
  "compilerOptions": {
    "target": "ES2020", // Define o nível de JavaScript de saída. "ES2020" é adequado para Node.js >= 18.
    "module": "CommonJS", // Node.js usa CommonJS como sistema de módulos.
    "strict": true, // Ativa todas as verificações de tipo estritas.
    "esModuleInterop": true, // Facilita a importação de módulos CommonJS, como o Express.
    "forceConsistentCasingInFileNames": true, // Garante consistência no uso de maiúsculas/minúsculas nos caminhos dos arquivos.
    "skipLibCheck": true, // Ignora a verificação de tipos em declarações de biblioteca para acelerar o build.
    "moduleResolution": "node", // Resolve módulos como o Node.js.
    "outDir": "./build", // Saída dos arquivos compilados para a pasta "build".
    "rootDir": "./src", // Define "src" como diretório raiz.
    "resolveJsonModule": true, // Permite importar arquivos JSON como módulos.
    "typeRoots": ["./node_modules/@types"], // Garante que o TypeScript encontre os tipos instalados.
    "allowSyntheticDefaultImports": true, // Suporte para importações padrão simuladas.
    "sourceMap": true, // Gera mapas de origem para depuração.
    "declaration": true // Gera arquivos .d.ts para facilitar o uso de tipos no futuro.
  },
  "include": ["src"], // Inclui apenas o diretório "src" no projeto.
  "exclude": ["node_modules", "build"] // Exclui a pasta "node_modules" e "build" do projeto.
}
